[build]
builder = "NIXPACKS"
# 不要写 /nixpacks.toml（绝对路径会找不到），相对路径即可
# nixpacksConfigPath = "nixpacks.toml"

[deploy]
startCommand = "python -m bot.main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 30
